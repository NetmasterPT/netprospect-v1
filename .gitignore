node_modules/
docker/.env
docker/.env.worker
docker/.data/
data/geoip/*.mmdb
data/proxies.json
config/verify-providers.json
config/verify-proxies.json
config/sending-accounts.json
out/*.txt
out/*.log
out/*.raw
out/*.progress.json
package-lock.json
data/
docker/docker-compose.yml.bak.premigrate
docker/.env.bak.premigrate
docker/docker-compose.repoint.yml

# Credenciais de acesso das VMs Oracle (chaves SSH privadas + IPs) — NUNCA no git
deploy/E2-micro-*/

# Auto-deploy por PULL — store de .env por host (segredos) + config/logs dos agentes
fleet-env/
deploy/agent/agent.env
deploy/agent/agent.env.ps1
deploy/agent/*.log

# Reacher (verificação SMTP) — .env do host (HELO/FROM/segredos do proxy)
deploy/reacher/.env
deploy/reacher/.proxy-pass

# Chaves de service-account / credenciais (nunca committar)
google.service-account.json
*.service-account.json
