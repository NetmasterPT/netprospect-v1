# Copiar de docker/.env do HEL1 (MESMAS credenciais → a app só troca o CLICKHOUSE_URL)
CLICKHOUSE_DB=netprospect
CLICKHOUSE_USER=netprospect
CLICKHOUSE_PASSWORD=

# IP tailnet DESTA VM (tailscale ip -4). Sem auth forte → NUNCA 0.0.0.0.
TAILNET_IP=

# --- PostHog (opt-in, só com 16 GB) — usa docker/posthog.compose.yml ---
# POSTHOG_SECRET_KEY=      # >= 32 chars
# POSTHOG_DB_PASSWORD=
